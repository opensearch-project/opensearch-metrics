enum Project{
    AWS_TEST = '',
    IAD = 'us-east-1',
    HOSTED_ZONE = 'insights.opensearch.org',
    // Temp until the project is public
    RESTRICTED_PREFIX = '',
    // Temp until the repo is public
    FRONTEND_CODE_S3_LOCATION = 's3://sample-bucket-us-east-1/opensearch-health-dashboard'
}
export default Project;